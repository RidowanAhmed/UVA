#include<bits/stdc++.h>
using namespace std ;
#define ull unsigned long long

ull dp[1001] = {0};

int main()
{
	int t;
	scanf("%d", &t);
	while(t--)
	{
		int n;
		scanf("%d", &n);
		for(int i=0 ; i<n ; i++)
			scanf("%llu", &dp[i]);
		llu sum = 0;
		int coin = 0;
		for(int i=0 ; i<n ; i++)
		{
			for(int j=0  ; j<n ; j++)
			{
				if()
			}
		}

		printf("%d\n", coin);
	}
	return 0 ;
}